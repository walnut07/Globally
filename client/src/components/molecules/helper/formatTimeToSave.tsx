const formatTimeToSave = (convertedStartTimes: string[], convertedEndTimes: string[], 
  attendeeAreas: string[], userDate: string, userStartTime: string, 
  userEndTime: string, userArea: string, user: any) => {
  
  // TODO: format time here.
  // The format is what's written in dataformat.json
    
}

export default formatTimeToSave;